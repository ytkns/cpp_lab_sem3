#pragma once

/**
 * @brief Inkrementacja zmiennej j i ustawienie wartości i.
 *
 * Funkcja zwiększa zmienną j o 1, ustawia zmienną i na 3,
 * a następnie wypisuje wynik, ograniczając go linią "----".
 */
void print1();

/**
 * @brief Inkrementacja zmiennych i oraz j.
 *
 * Funkcja zwiększa zmienne i i j o 1, a następnie wypisuje
 * wynik ograniczając go linią "----".
 */
void print2();

/**
 * @brief Inkrementacja zmiennych i, j i k oraz wywołanie print2.
 *
 * Funkcja zwiększa zmienne i, j i k o 1, wywołuje funkcję print2,
 * a następnie wypisuje wynik ograniczając go linią "----".
 */
void print3();